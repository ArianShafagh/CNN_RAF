# --- PyTorch with MPS (Apple Silicon) ---
# Install the whisky/py3.14-compatible wheels. For macOS arm64:
#   pip install -r requirements.txt
# Stable torch supports MPS on arm64. If you hit a Python 3.14 wheel gap,
# fall back to torch nightly: pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cpu

torch>=2.4.0
torchvision>=0.19.0
onnx>=1.16.0
onnxruntime>=1.18.0

# --- live webcam demo ---
# Pinned below 5.0: OpenCV 5 removed the bundled Haar cascade XML files used by
# the --detector haar fallback.
opencv-python>=4.9.0,<5.0.0

# Face detection for the webcam demo (BlazeFace short range).
# Pinned to 0.10.x: mediapipe 1.0.1 aborts on macOS arm64 inside
# TensorsToDetectionsCalculator ("Check failed: service_ Service is unavailable").
# Install with --no-deps, because its dependency on opencv-contrib-python 5.x
# would replace the opencv-python 4.x pinned above and delete the cascades:
#     pip install --no-deps 'mediapipe==0.10.35'
#     pip install absl-py sounddevice cffi pycparser certifi
mediapipe==0.10.35

# --- data / viz / metrics ---
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
Pillow>=10.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
